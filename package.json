{
  "name": "scrcpy-desktop",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src-server/server.js",
    "dev": "parcel public/index.html --dist-dir public/dev-dist --public-url /",
    "build": "parcel build public/index.html --dist-dir public/dist --public-url ./",
    "clean": "rimraf ./public/dist ./public/dev-dist ./.parcel-cache",
    "rebuild": "npm run clean && npm run build"
  },
  "keywords": [],
  "author": "serifpersia",
  "license": "MIT",
  "description": "Web application for scrcpy screen and audio streaming.",
  "dependencies": {
    "@devicefarmer/adbkit": "^3.3.8",
    "express": "^5.1.0",
    "h264-converter": "^0.1.4",
    "nanoid": "^5.1.5",
    "node-dns-sd": "^1.0.1",
    "qrcode": "^1.5.4",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "parcel": "^2.14.4",
    "rimraf": "^5.0.10",
    "svgo": "^3.3.2"
  }
}
